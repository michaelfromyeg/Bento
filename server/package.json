{
    "name": "server",
    "version": "1.0.0",
    "description": "Backend for my custom Bento box.",
    "main": "server.js",
    "scripts": {
        "start": "fastify start -a 0.0.0.0 -l info src/app.js",
        "dev": "fastify start -l info -P src/app.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "deploy": "cd ..; git subtree push --prefix server heroku main",
        "format": "prettier . --write"
    },
    "author": "Michael DeMarco <michaelfromyeg@gmail.com>",
    "license": "GPL-3.0",
    "dependencies": {
        "fastify": "^3.25.3",
        "fastify-cli": "^2.14.0",
        "fastify-cors": "^6.0.2",
        "node-fetch": "^2.6.6"
    },
    "devDependencies": {
        "@tsconfig/node16": "^1.0.2",
        "@types/node": "^17.0.5"
    },
    "engines": {
        "node": "16.13.1"
    }
}
